# Copy to .env and fill in. Never commit real secrets (SPEC §8).

# Weights & Biases. Use offline mode in CI / when you have no account.
WANDB_API_KEY=
WANDB_MODE=offline
WANDB_PROJECT=vlm-distill-screenshots

# Hugging Face Hub token (needed for gated models / pushing weights).
HF_TOKEN=
