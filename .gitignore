# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
.env

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Large artifacts — never commit weights / datasets / run outputs.
# Anchored to repo root so they don't shadow the src package (e.g. the
# `src/vlm_distill/data/` directory must NOT be ignored).
/results/
/data/
/wandb/
/checkpoints/
*.parquet
*.arrow
*.onnx
*.safetensors
*.bin

# OS / editor
.DS_Store
.idea/
.vscode/
